version: "3.8"

services:
  mongo:
    image: mongo:latest
    container_name: mongo
    restart: always
    ports:
      - "27017:27017"

  # app:
  #   build: .
  #   ports:
  #     - "8000:3000"
  #   environment:
  #     - SESSION_SECRET=secret
  #     - ENVIRONMENT=production
