import { progressTime, countDown } from "./date";

describe("progressTime", async () => {
  test("Over 100", async () => {});
  test("Around 50", async () => {});
  test("Hasn't started yet", async () => {});
});

describe("countDown", async () => {
  test("Month leading", async () => {});
  test("Day leading", async () => {});
  test("Hour leading", async () => {}); //might be hard to be exact w/seconds
  test("Day has passed", async () => {});
});
