{
  "name": "mdao-indexer",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development tsx -r dotenv/config indexer/run.ts",
    "start:indexer": "cross-env NODE_ENV=production tsx indexer/run.ts",
    "lint": "eslint --cache --cache-location ./node_modules/.cache/eslint '**/*.{ts,tsx}'",
    "reindex": "cross-env NODE_ENV=production tsx indexer/reindex.ts"
  },
  "dependencies": {
    "@mdao/schema": "*",
    "@mdao/database": "*",
    "@mdao/pinekit": "*",
    "cross-env": "^7.0.3",
    "dotenv": "^16.0.1",
    "ethers": "^5.7.2",
    "tiny-invariant": "^1.3.1",
    "winston": "^3.8.2",
    "tsx": "^3.11.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=15"
  }
}
